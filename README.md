# Rompressor
Powershell script to convert existing Roms (including compressed) to .7z archives to reduce disk usage.

# USAGE
**Currently $temp and $sevenzip are hardcoded paths so these will need to be updated for your configuration.**

1) run the script
2) select the source path
3) ???
4) profit
